import { PageShell, StubPlaceholder } from "@/components/PageShell";

export default function CalendarPage() {
  return (
    <PageShell
      title="Calendar"
      description="Weekly grid with gap alerts"
    >
      <StubPlaceholder phase="v2 (deferred from v1)" />
    </PageShell>
  );
}
