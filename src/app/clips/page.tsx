import { PageShell, StubPlaceholder } from "@/components/PageShell";

export default function ClipsPage() {
  return (
    <PageShell
      title="Clips"
      description="Short-form Quran recitation clips for X, Reels, Facebook"
    >
      <StubPlaceholder phase="Phase 5 (highest-ROI feature)" />
    </PageShell>
  );
}
