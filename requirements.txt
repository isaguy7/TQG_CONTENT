# TQG Content Studio — Python dependencies
# Phase 1: video download + transcription

# Video download. Breaks roughly monthly when YouTube changes.
# Upgrade via Settings → "Upgrade yt-dlp" button (pip install --upgrade yt-dlp)
yt-dlp>=2026.3.0

# WhisperX — transcription + word-level alignment via wav2vec2.
# IMPORTANT: install a CUDA-enabled PyTorch FIRST (see README).
# The default `pip install torch` pulls the CPU build, which will fail
# at runtime on an RTX 4090 with "Torch not compiled with CUDA enabled".
whisperx>=3.1

# Faster backend used internally by whisperx.
faster-whisper>=1.0.0
