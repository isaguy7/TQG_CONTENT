# TQG Content Studio — Python dependencies
# Phase 1: video download + transcription

# Video download. Breaks roughly monthly when YouTube changes.
# Upgrade via Settings → "Upgrade yt-dlp" button (pip install --upgrade yt-dlp)
yt-dlp==2026.3.31

# WhisperX — transcription + word-level alignment via wav2vec2
# Requires CUDA 12.x + cuDNN + PyTorch with CUDA support
whisperx==3.4.3

# Faster backend for whisper inference (used by whisperx internally)
faster-whisper>=1.1.0

# Torch is pulled in by whisperx; if you need to pin for your CUDA version,
# install torch explicitly first per pytorch.org instructions, then run
# `pip install -r requirements.txt` with torch already satisfied.
